[project]
name = "code-search-tool-backend"
version = "0.1.0"
description = "FastAPI backend for Code Search Tool"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.41.0",
]
