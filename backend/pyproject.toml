[project]
name = "code-search-tool-backend"
version = "0.1.0"
description = "FastAPI backend for Code Search Tool"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "groq>=1.1.1",
    "sse-starlette>=3.3.2",
    "uvicorn>=0.41.0",
]
